import { Annuity, Divide, Faucet, Mine, Perpetuity, Record   } from "../index"

export const contractMap = {
    A : Annuity,
    D : Divide,
    F : Faucet,
    M : Mine,
    P : Perpetuity,
    R : Record
}